# Backend Python Dependencies

# Django Framework
Django==5.2

asgiref==3.8.1  # ASGI framework, dependency for Django and Uvicorn/Daphne

# AI & LLM Libraries
dspy==2.6.24      # DSPy framework (assuming this is the correct package for 'import dspy')
litellm==1.72.0   # LLM gateway

# File Processing
PyPDF2==3.0.1
python-docx==1.1.2  # For .docx file processing (imported as 'docx')

# Environment & Async
python-dotenv==1.1.0

# HTTP Server & Client
uvicorn[standard]~=0.29.0 # ASGI server (includes websockets and http tools)
requests==2.32.3          # For synchronous HTTP requests 
httpx==0.28.1             # For asynchronous HTTP requests 
# Install the Libraries Required for Your own Databases


